import 'package:flutter/material.dart';

class VideoRouter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      child: Text("视频"),
    );
  }
}
